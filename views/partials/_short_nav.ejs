<div class="nav-wrap short">
  <header class="top-nav short">
    <a class="brand" href="/"><span class="blue">Map</span> <span class="red">Attack</span></a>
    <% if (authenticated) { %>
    <nav class="left">
      <!-- <a href="/editor" class="btn blue">editor</a> -->
    </nav>
    <% } %>

    <nav class="right">
    <% if (authenticated && user) { %>
      <img src="<%= user.photos[0].value %>" alt="<%= user.username %>" width="42">
      <a href="/logout" class="btn red">logout</a>
    <% } else { %>
      <a href="/auth/twitter" class="blue-link">Log In</a>
    <% } %>
    </nav>
  </header>
</div>
